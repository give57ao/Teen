<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.ez.login.mapper.LoginMapper">
 
 
 <insert id="registNewMember">
 insert into member(id, password, name, email, zipcode, address)
values(#{id}, #{password}, #{name}, #{email}, #{zipcode}, #{address})
 </insert>
 
 <select id="login" resultType="com.ez.login.model.MemberModel">
		SELECT id, password 
		  FROM member
		 WHERE id = #{id}
		   AND password = #{password}   
	</select>
 
  </mapper>